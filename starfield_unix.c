#include <stdio.h>\n#include <stdlib.h>\n#include <time.h>\n\n// Function to print the starfield animation\nvoid printStarfield(int width, int height) {\n    // Clear the console\n    printf("\033[H\033[J");\n    for (int i = 0; i < height; i++) {\n        for (int j = 0; j < width; j++) {\n            // Randomly decide to place a star\n            if (rand() % 100 < 5) {  // 5% chance\n                printf("* ");\n            } else {\n                printf("  ");\n            }\n        }\n        printf("\n");\n    }\n}\n\nint main() {\n    srand(time(NULL));  // Seed for randomness\n    int width = 80;  // Width of the console\n    int height = 24; // Height of the console\n\n    while (1) {\n        printStarfield(width, height);\n        usleep(100000); // Sleep for 100 milliseconds\n    }\n    return 0;\n}